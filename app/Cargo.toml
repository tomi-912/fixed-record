[package]
name = "app"
version = "0.1.0"
edition = "2024"

[features]
unchecked = ["fixed_record_main/unchecked"]

[dependencies]
fixed_record_main = { path = "../fixed_record_main" }

[dev-dependencies]
trybuild = "1.0"
