[package]
name = "fixed-record-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[features]
default = ["list"]
list = []
unchecked = []

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
heck = "0.4"
