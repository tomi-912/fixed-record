[package]
name = "fixed-record-basic-example"
version = "0.1.0"
edition = "2024"
license = "MIT"

[features]
unchecked = ["fixed-record/unchecked"]

[dependencies]
fixed-record = { path = "../../crates/fixed-record" }
