[package]
name = "fixed-record-no-list-example"
version = "0.1.0"
edition = "2024"
license = "MIT-0"

[dependencies]
fixed-record = { path = "../../crates/fixed-record", default-features = false }

[dev-dependencies]
trybuild = "1.0"
